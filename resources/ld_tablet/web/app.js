const tablet=document.getElementById('tablet');const closeBtn=document.getElementById('close');const timeEl=document.getElementById('time');window.addEventListener('message',(event)=>{const data=event.data||{};if(data.action==='open'){tablet.classList.remove('hidden')}if(data.action==='close'){tablet.classList.add('hidden')}if(data.action==='reset'){localStorage.removeItem('ld_tablet_settings')}});closeBtn.addEventListener('click',()=>{fetch(`https://${GetParentResourceName()}/closeTablet`,{method:'POST',body:JSON.stringify({})})});document.addEventListener('keydown',(e)=>{if(e.key==='Escape'){fetch(`https://${GetParentResourceName()}/closeTablet`,{method:'POST',body:JSON.stringify({})})}});setInterval(()=>{const now=new Date();timeEl.textContent=now.toLocaleTimeString('de-DE',{hour:'2-digit',minute:'2-digit'})},1000);